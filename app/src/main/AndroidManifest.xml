<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SDP_mobile_app"
        tools:targetApi="31">
        <activity
            android:name=".teacher.MyDisciplineActivity"
            android:exported="false" />
        <activity
            android:name=".MainTeacherActivity"
            android:exported="false"
            android:label="@string/title_activity_main_teacher"
            android:theme="@style/Theme.SDP_mobile_app.NoActionBar" />
        <activity
            android:name=".regist.ui.Register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".regist.ui.forgetpw.ForgetActivity"
            android:exported="false" />
        <activity
            android:name=".regist.ui.login.LoginActivity"
            android:exported="true"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SDP_mobile_app.NoActionBar" />
        <activity
            android:name=".admin.FacultyActivity"
            android:label="FacultyActivity" />
        <activity
            android:name=".admin.GroupActivity"
            android:label="GroupActivity" />
        <activity
            android:name=".admin.EdudirActivity"
            android:label="ProgrammeActivity" />
        <activity
            android:name=".admin.DisciplineActivity"
            android:label="CourseworkActivity" />
    </application>

</manifest>